VITE_BACKEND_URL=http://localhost:8001

# Stripe price ids are NOT set here. The backend owns the plan-to-price mapping
# (STRIPE_PRICE_ID_MONTHLY / STRIPE_PRICE_ID_ANNUAL on Railway) so the browser
# can't name its own price, and so prices change without a frontend rebuild.
